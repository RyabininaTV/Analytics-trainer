quarkus:
  http:
    host: 0.0.0.0
    port: 8080
  datasource:
    db-kind: postgresql
    jdbc:
      url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jooq:
    dialect: POSTGRES
